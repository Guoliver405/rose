# Supabase
.branches
.temp

# Wird aus Supabase_sql/archive/ erzeugt (scripts/sync-migrations.mjs, läuft in
# npm run db:reset). Einzige Quelle der Wahrheit bleibt die Projekt-Ablage —
# hier läge sonst jede Migration ein zweites Mal im Repo.
migrations/

# dotenvx
.env.keys
.env.local
.env.*.local
